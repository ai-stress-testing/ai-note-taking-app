# <Feature name> — design

## Approach

How this gets built, in prose. What in `src/lib/store.ts`, `src/lib/commands.ts`,
`src/routes/`, or `src/components/` changes or gets added.

## Data model changes

New/changed types, store fields, actions.

## Alternatives considered

What else was considered and why it was rejected.

## Open questions

Anything still undecided that implementation will need to resolve.
